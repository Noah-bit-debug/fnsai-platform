node_modules/
dist/
.env
.env.local
.env.production
.DS_Store
*.log
.vercel

# Generated by scripts/copy-pdfjs-assets.mjs (runs as prebuild/predev).
# Always sourced from node_modules/pdfjs-dist so the version stays
# locked to the package.json dep — committing it would risk drift.
public/pdfjs/
